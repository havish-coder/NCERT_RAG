# ── LLM — online inference via Ollama (OpenAI-compatible endpoint) ─────────────
openai==1.84.0

# ── CLI ───────────────────────────────────────────────────────────────────────
rich==13.9.4

# ── LLM — offline HuggingFace inference (Kaggle / Lightning AI notebooks) ──────
transformers==4.51.3
accelerate==1.6.0

# ── Database clients ──────────────────────────────────────────────────────────
neo4j==5.28.4
qdrant-client==1.14.3

# ── PDF parsing ───────────────────────────────────────────────────────────────
PyMuPDF==1.27.2.3

# ── Embeddings — BAAI/bge-m3 (hybrid dense + sparse) ─────────────────────────
# Install PyTorch with CUDA BEFORE this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
FlagEmbedding==1.3.5
torch>=2.3.0

# ── Data I/O ──────────────────────────────────────────────────────────────────
pandas==2.2.3
pyarrow==19.0.1
numpy==1.26.4

# ── Graph / Community detection ───────────────────────────────────────────────
python-igraph==0.11.9
leidenalg==0.10.2

# ── Tokenization ──────────────────────────────────────────────────────────────
tiktoken==0.9.0

# ── Async HTTP ────────────────────────────────────────────────────────────────
httpx==0.28.1
aiofiles==24.1.0

# ── Config & validation ───────────────────────────────────────────────────────
pydantic==2.11.9
pydantic-settings==2.9.1
python-dotenv==1.2.2

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==26.1.0

# ── Retry logic ───────────────────────────────────────────────────────────────
tenacity==9.1.4

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.67.1
