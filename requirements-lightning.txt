# Minimal deps for the OFFLINE ingestion job on Lightning AI (run_pipeline.py).
# torch + transformers + accelerate usually ship on Lightning GPU images already;
# they are listed for completeness / non-GPU images.

# LLM extraction (HuggingFace)
# Pin transformers: FlagEmbedding 1.3.5 imports is_torch_fx_available, which
# was removed in transformers >=4.52. Lightning ships a newer one by default,
# so pin to the version verified locally.
transformers==4.51.3
accelerate>=0.30.0

# Embeddings — BAAI/bge-m3 (dense + sparse + ColBERT for late chunking)
FlagEmbedding==1.3.5

# PDF parsing
PyMuPDF==1.27.2.3

# Tokenization
tiktoken==0.9.0

# Data I/O
pandas>=2.0
pyarrow>=15.0
numpy<2.0

# Graph / community detection
python-igraph==0.11.9
leidenalg==0.10.2

# Config / logging / progress
pydantic>=2.0
pydantic-settings>=2.0
structlog>=24.0
tqdm>=4.66
